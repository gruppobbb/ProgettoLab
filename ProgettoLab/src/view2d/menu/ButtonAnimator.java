package view2d.menu;

/**
 * Interfaccia per la definizione di una animazione per un {@link MenuButton}.
 * @author Jancarlos
 */
public interface ButtonAnimator {
	
	/**
	 * Metodo per l'esecuzione dell'animazione.
	 * @param running
	 */
	public void runAnimation(boolean running);

}
